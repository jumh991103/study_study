# 총정리의 경우 책 2회독째부터 다시 모르는것들만 적을거임. 아는 건 안적을거임.

# 2장
1. format() 함수의 다양한 기능
    1.1 정수 출력의 다양한 형태
        - output_a = "{:d}".format(53)  ->:d는 매개변수로 정수만 받겠다.
        - output_b = "{:5d}.format(53)  -> d앞의 숫자는 그만큼 빈칸을 잡고 뒤에부터 숫자를 채우겠다는 소리임(이건 그럼 53앞에 4칸의 빈칸이 있다는 소린가?)
        - output_c = "{:05d}.format(53) -> d앞의 숫자 만큼 빈칸을 잡고 뒤에부터 숫자를 채운다음 남은 빈칸을 9으로 채운다는 소리임. 
                                           만약 입력받은 정수가 음수라면 맨앞자리 부호로 채우고 나머지를 0으로 채우는 것.

        - output_d = "{:+d}.format(53)  -> d앞에 +기호를 두면 양수와 음수 기호를 붙일 수 있음. 예를 들어 변수로 53을 받으면 +53이 출력되고 -53을 받으면 -53이 출력된다.
        - output_e = "{: d}.format(53)  -> d앞에 공백을 두어 기호 위치를 비워 주면 변수로 받은 기호가 출력됨. 대신 여기선 +의 경우 +는 표시안되고 대신 공백이 출력됨.
        - output_f = "{: d}.format(-53) -> -의 경우 -가 표현됨.

        -- output_g = "{:+5d}.format(53)  -> 앞에 공백2개에다가 +53 출력
        -- output_g = "{:+5d}.format(-53) -> 앞에 공백2개에다가 -53 출력
        -- output_g = "{:=+5d}.format(53) -> 기호가 맨앞으로 감. 공백보다 앞에따라서 "+ 공백 공백 53" 출력
        -- output_g = "{:=+5d}.format(-53)-> "- 공백 공백 53" 출력
        -- output_g = "{:+05d}.format(53)  -> 앞에 "0"2개에다가 +53출력 ->틀렷음. 기호가 가장 앞에 옴 -> 0도 숫자 취급하기 때문인가?
        -- output_g = "{:+05d}.format(-53) -> "-0053"출력

    1.2 부동 소수점 출력의 다양한 형태
        - output_a = "{:f}".format(53.333) -> :f는 매개변수로 실수만 받겠다.
        - output_b = "{:15f}".format(53.333) -> 앞에 15칸 빈칸하고 뒤에부터 채우기
        - output_c = "{:+15f}".format(53.333) -> 앞에 15칸 빈칸하고 기호와 숫자를 뒤에서 부터 채우기
        - output_d = "{:+015f}".format(53.333) -> 앞에 15칸 빈칸하고 기호 맨앞에 두고 뒤에부터 숫자 채운뒤 나머지 빈칸은 0으로 채우기

        - output_e = "{:15.2f}.format(53.333) -> 15칸 빈칸으로 두고 뒤에부터 소수점 2자리까지 표현하기

        - output_f = "{:g}.format(53.3330) -> :g는 의미없는 0을 제거(0만 제거하는거지? 소수점 아래 다 제거가아니라?)

2. 대소문자 바꾸기
    a.upper(), a.lower() 형식으로 사용. -> a문자열 원본이 바뀌는게 아님!!!!

3. 문자열 양옆 공백 제거
    a.strip형식으로 사용(왼쪽만 제거하고 싶으면 lstrip 사용)

4. 문자열의 구성 파악하기
    is를 붙인함수들임. ->참고로 결과값이 True,False 즉 boolean 형으로 나옴.
    a.isalnum() -> 알파벳또는 숫자로만 이루어져있는지
    a.isalpha() -> 알파벳으로만 이루어져있는지
    a.isidentifier() -> 식별자로 사용할 수 있는 것인지
    a.isdecimal() -> 정수형태인지
    a.isspace() -> 공백으로만 이루어져있는지
    a.digit() -> 숫자로 인식될 수 있는 문자열인지
    a.islower() -> 소문자로만 이루어져있는지
    a.isupper() -> 대문자로만 이루어져있는지

5. 문자열 찾기
    a.find() $ a.rfind() -> 문자열 내부에 특정문자가 어디에 위치하는지 확인. 결과값으로 인덱스 번호가 나온다.

6. 문자열 in 연산자
    문자열 in 문자열 -> boolean을 리턴함.

7. 문자열 자르기
    a.split("") -> 리스트를 리턴함.

# 3장
1. boolean 연산자 -> not, and, or
2. 파이썬에서 False : None / 숫자 0과 0.0 / 빈컨테이너(빈리스트 등등)

# 4장
## 리스트 (list)

1. 변경 -> `list이름[인덱스] = 바꿀값`

2. 요소 추가
   1) `list이름.append(요소)` -> 요소 하나만 추가 가능
   2) `list이름.insert(인덱스번호, 요소)` -> 어느 위치에 추가할지 지정 가능
   3) `list이름.extend(요소들)` -> extend는 인풋으로 받은 걸 하나씩 순회하면서 추가함.
      그래서 두 개의 문자를 추가하고 싶으면 `'hi'`처럼 넣으면 `'h'`, `'i'` 두 개가 각각 추가되고,
      list 형식으로 넣으면 그 list 안에 있는 것들을 하나씩 뽑아서 추가해줌

3. 요소 제거
   1) 인덱스로 제거하기
      - `del list명[인덱스]` -> `[::]`로 범위 지정해서 한꺼번에 삭제 가능
      - `list명.pop(인덱스)`
   2) 값으로 제거하기 -> `list명.remove(값)` -> 왼쪽부터 시작해서 가장 처음 만나는 그 값을 지움
   3) 모두 제거하기 -> `list명.clear()`

4. 요소 정렬 -> `list.sort()` -> `list.sort(reverse=True)`라고 하면 역순으로 정렬함 & 원본을 바꿈.

   > `sorted(이터러블)` -> 이터러블들에 모두 적용가능 함수 & 리스트를 반환함.
5. 내부에 요소 있는지 확인 -> `값 in list명`

6. 전개 연산자 -> `*리스트명` -> 리스트를 하나씩 풀어서 전개한 것과 같음.
   extend랑 다른 건, extend는 호출한 리스트의 원본을 바꿔버리지만 얜 잠깐 풀어서 확인하는 정도임.
   그래서 `c=[*a]`를 해도 a의 요소들을 풀어서 c라는 리스트로 만든 것이지 a가 하나씩 풀린 건 아님

---

## 딕셔너리 (dict)

1. 요소에 접근 -> `dict이름[키]` -> 딕셔너리도 인덱스로 확인 가능하기 때문에 `[]`로 접근함

2. 요소 변경 -> `dict이름[가지고 있는 키] = 새로운 값`

3. 요소 추가 -> `dict이름[새로운 키] = 새로운 값`

4. 요소 제거 -> `del dict이름[키]`

5. 내부에 요소(키) 있는지 확인
   1) `키 in dict이름`
   2) `dict이름.get(키)` -> 존재하지 않으면 None 출력

6. 딕셔너리의 경우 for문을 사용할 때 딕셔너리의 요소 전부(키와 밸류값)가 모두 변수에 가는 게 아니라 **키만** 변수에 들어감 !!!! 중요

```python
dic = {"a": 1, "b": 2}
for k in dic:
    print(k)   # a, b  (키만 나옴)

# 키와 값을 둘 다 쓰고 싶으면 .items() 사용
for k, v in dic.items():
    print(k, v)   # a 1 / b 2
```

---

## 세트 (set)

1. 변경 -> ❌ 불가능. set은 "순서가 없는" 자료형이라 인덱스 자체가 없음. 그래서 특정 요소만 콕 집어서 바꾸는 방법이 없음

2. 요소 추가
   1) `set이름.add(요소)` -> 요소 하나만 추가 가능 (이미 있는 값이면 조용히 무시됨, 중복 불가라서)
   2) `set이름.update(요소들)` -> extend처럼, 인풋으로 받은 걸 하나씩 순회하면서 추가함.
      `update(['a','b'])`처럼 넣으면 `'a'`, `'b'` 각각 추가됨

3. 요소 제거
   1) `set이름.remove(값)` -> 그 값이 없으면 에러(`KeyError`) 남
   2) `set이름.discard(값)` -> 그 값이 없어도 에러 안 남 (더 안전한 버전)
   3) `set이름.clear()` -> 모두 제거

4. 요소 정렬 -> ❌ set 자체엔 정렬 메소드가 없음 (애초에 순서가 없는 자료형이라서).
   정렬된 결과가 필요하면 `sorted(set이름)`을 쓰면 리스트로 변환되어 나옴

5. 내부에 요소 있는지 확인 -> `값 in set이름`

6. 전개 연산자 -> `*set이름` -> 다른 것들과 마찬가지로 하나씩 풀어서 전개함.
   단, set은 순서가 없어서 풀리는 순서가 매번 보장되지 않을 수 있음

7. set만의 특별한 연산 (교집합/합집합/차집합)

```python
a = {1, 2, 3}
b = {2, 3, 4}

a & b   # 또는 a.intersection(b)   -> 교집합 {2, 3}
a | b   # 또는 a.union(b)          -> 합집합 {1, 2, 3, 4}
a - b   # 또는 a.difference(b)     -> 차집합 {1}
```

---

## 튜플 (tuple)

1. 변경 -> ❌ 불가능. 튜플은 "불변(immutable)" 자료형이라서 한 번 만들면 안의 값을 절대 못 바꿈

```python
t = (1, 2, 3)
t[0] = 10   # TypeError!
```

2. 요소 추가 -> ❌ 불가능. `append`, `insert`, `extend` 전부 없음 (불변이라서 애초에 "추가"라는 개념 자체가 성립 안 함)

3. 요소 제거 -> ❌ 불가능. `remove`, `pop`, `clear` 전부 없음 (같은 이유)

4. 요소 정렬 -> ❌ 튜플 자체엔 `sort()` 없음. 정렬된 결과가 필요하면 `sorted(tuple이름)`을 쓰면 리스트로 변환되어 나옴

5. 내부에 요소 있는지 확인 -> `값 in tuple이름` (조회만 하는 거라 가능)

6. 전개 연산자 -> `*tuple이름` -> 다른 것들과 마찬가지로 하나씩 풀어서 전개 가능 (조회 성격이라 가능)

7. 튜플은 뭘 할 수 있나 -> 조회(인덱싱/슬라이싱)만 가능

```python
t = (1, 2, 3, 4)
t[0]      # 1     -> 조회는 됨 (읽기는 가능, 쓰기만 안 됨)
t[0:2]     # (1, 2) -> 슬라이싱도 조회니까 가능
```

---

## 🔑 4개 자료형 한눈에 비교표

| | 리스트 | 튜플 | 딕셔너리 | 세트 |
|---|---|---|---|---|
| 순서 있음? | ✅ | ✅ | ✅ (파이썬 3.7+) | ❌ |
| 중복 허용? | ✅ | ✅ | 키는 ❌ / 값은 ✅ | ❌ |
| 변경 가능(mutable)? | ✅ | ❌ | ✅ | ✅ |
| 인덱스로 접근(`[0]`) | ✅ | ✅ | ❌ (키로 접근) | ❌ |
| 요소 추가 메소드 | `append`, `insert`, `extend` | 없음 | `dict[새키]=값` | `add`, `update` |
| 요소 제거 메소드 | `del`, `pop`, `remove`, `clear` | 없음 | `del` | `remove`, `discard`, `clear` |
| 정렬 | `.sort()` (원본 변경) | 없음 (`sorted()`만) | 없음 (`sorted()`만) | 없음 (`sorted()`만) |
| 만드는 기호 | `[ ]` | `( )` | `{ 키:값 }` | `{ 값 }` |

### 왜 이렇게 다르게 설계됐는지 (핵심 감각)

- **리스트**: 자주 바뀌는 순서 있는 데이터 → 뭐든 다 됨 (추가/삭제/변경/정렬)
- **튜플**: 절대 안 바뀌어야 하는 순서 있는 데이터 → 조회만 됨, 그 대신 가볍고 안전함
- **딕셔너리**: 키로 의미를 부여해서 찾아야 하는 데이터 → 인덱스 대신 키로 접근
- **세트**: 중복 없이 존재 여부만 중요한 데이터 → 순서·인덱스 개념 자체가 없고, 대신 교집합/합집합 같은 집합 연산이 특화됨

### for문 순회 시 차이

```python
# 리스트, 튜플은 for문 돌리면 값이 그대로 하나씩 나옴
for i in [1, 2, 3]:
    print(i)   # 1, 2, 3

for i in (1, 2, 3):
    print(i)   # 1, 2, 3

# 세트도 값이 하나씩 나오지만, 순서가 보장 안 됨
for i in {1, 2, 3}:
    print(i)   # 순서는 매번 같을 수도 다를 수도 있음

# 딕셔너리는 기본적으로 "키"만 나옴 (주의!)
for k in {"a": 1, "b": 2}:
    print(k)   # a, b
```